image: golang:1.24

stages:
#  - lint
#  - test
  - build


default:
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  before_script:
    - docker info

variables:
  DOCKER_TLS_CERTDIR: "/certs"

build:
  stage: build
  script:
    - docker build -t todo_app -f ./Dockerfile .

#lint:
#  stage: lint
#  image: golangci/golangci-lint:latest
#  allow_failure: true
#  script:
#    - golangci-lint run
#
#test:
#  stage: test
#  image: golang:1.24
#  script:
#    - go test -v ./...



